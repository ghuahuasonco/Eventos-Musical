<%= form_for(@musical_style) do |f| %>
  <% if @musical_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@musical_style.errors.count, "error") %> prohibited this musical_style from being saved:</h2>

      <ul>
      <% @musical_style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
